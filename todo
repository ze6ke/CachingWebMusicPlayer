//add everything to git
//setup lint
//setup watcher (do I need gulp)
//nodemon not restarting with server webpack execution (http://jlongster.com/Backend-Apps-with-Webpack--Part-II)
//watch task and lint tasks are doing both client and server
//setup basic player
//setup unit test suite
//run it on my phone
//lint errors don't kill gulp task and leave nodemon running
//autoPlay
//design UI
//filtering
//play next song
//learn how manifests work
//get phone app to run in background
//test on dolphin--plays in background and goes to next song, needs to be open during download, a little finicky about partial downloads
//test on safari--doesn't play in background
//better express logger
//learn to read mp3 embedded data
//host music files directly from library
//autogen library.json
//autogen manifest file
//test in background for 1+ hours (started at 22:22) --nice results in dolphin
//test effect on battery life --nice results in dolphin
//make it look good
//use react lint and not inferno lint
//make initial load better (pull down library, but only display songs after the data has arrived)
//Move unit tests to karma
//fix unit test on prep sub-project (should it have a second package.json?)
//make watch and eslint work in text editor
//remove blob-util
//cache to localstorage (breakup files) (gives quota errors on all browsers that I tested, but stores the smallest song)
//storage as strategy
//cache to indexedDB
//create a trivial indexedDB example and confirm that it works on my phone
//create cache manifest experiments
//fix karma tests
//read
//understand font awesome better and why it loads late/or not at all
//behavior when list is fully filtered (have a no current song option)

test with larger list of files
if needed, try out persistent storage options
make gulp go faster
sorting
shuffle
https://www.npmjs.com/package/gulp-autoprefixer

make ctags work with vim https://github.com/andreypopp/ctags-webpack-plugin
dynamically determine header.height and set headerplaceholder.height to match
test on chrome
test on firefox
test on other browsers
setup istanbul for code coverage
create webpack bundle for libraries
move to nginx
make jumping ahead work on phone
try out different css templates
try out bootstrap
really make it look good (colors and stuff)
optimize how the long list is displayed (if necessary)
handle ogg files better (metadata doesn't work)
come up with good stylelint rules
how to deploy node product
setup integration test suite (nightshade or selenium directly)
open source
